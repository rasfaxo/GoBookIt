###############################################################################
# GoBookIt Environment Example
# Salin file ini menjadi `.env.local` untuk pengembangan lokal.
# JANGAN commit file .env.local atau nilai rahasia ke repository publik.
# Hanya variabel yang diawali NEXT_PUBLIC_ yang diekspos ke browser.
###############################################################################

# -----------------------------------------------------------------------------
# Appwrite - Public (Client + Server)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
NEXT_PUBLIC_APPWRITE_PROJECT=YOUR_APPWRITE_PROJECT_ID
NEXT_PUBLIC_APPWRITE_DATABASE=YOUR_DATABASE_ID
NEXT_PUBLIC_APPWRITE_COLLECTION_ROOMS=rooms
NEXT_PUBLIC_APPWRITE_COLLECTION_BOOKINGS=bookings
# Bucket (jika pakai storage untuk gambar)
NEXT_PUBLIC_APPWRITE_STORAGE_BUCKET_ROOMS=rooms

# OAuth redirect (opsional)
NEXT_PUBLIC_APPWRITE_OAUTH_SUCCESS_URL=http://localhost:3000/oauth/complete
NEXT_PUBLIC_APPWRITE_OAUTH_FAILURE_URL=http://localhost:3000/login

# Base URL (opsional override)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Bypass Next/Image optimization untuk Appwrite (1 aktif, kosong/non-1=nonaktif)
NEXT_PUBLIC_BYPASS_IMAGE_OPT=1

# -----------------------------------------------------------------------------
# Appwrite - Server Only (JANGAN tambah NEXT_PUBLIC_)
# Pilih salah satu nama kunci API yang akan dipakai (APPWRITE_API_KEY direkomendasikan).
# -----------------------------------------------------------------------------
APPWRITE_API_KEY=YOUR_SECURE_APPWRITE_API_KEY
# NEXT_APPWRITE_API_KEY=YOUR_SECURE_APPWRITE_API_KEY_ALT
# NEXT_APPWRITE_KEY=YOUR_SECURE_APPWRITE_API_KEY_ALT
# APPWRITE_ENDPOINT=OPTIONAL_OVERRIDE_ENDPOINT
# APPWRITE_PROJECT=OPTIONAL_OVERRIDE_PROJECT_ID

# -----------------------------------------------------------------------------
# Node / Next.js Standard
# -----------------------------------------------------------------------------
# NODE_ENV otomatis di-set oleh Next.js saat build/production.

# -----------------------------------------------------------------------------
# Placeholder tambahan (komentari jika tidak dipakai)
# -----------------------------------------------------------------------------
# NEXTAUTH_SECRET=your_nextauth_secret
# NEXTAUTH_URL=http://localhost:3000
# NEXT_PUBLIC_API_URL=http://localhost:3000/api

###############################################################################
# Catatan:
# - JANGAN pernah menambahkan nilai asli API key di file contoh ini.
# - Setiap perubahan env di Vercel: lakukan redeploy agar nilai diterapkan.
# - Simpan production secret hanya di dashboard Vercel (Environment Variables).
###############################################################################
